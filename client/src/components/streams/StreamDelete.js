import React from "react";
import Modal from "../Modal";

const StreamDelete = () => {
  return (
    <div>
      Stream Delete
      <div>
        <Modal />
      </div>
    </div>
  );
};

export default StreamDelete;
